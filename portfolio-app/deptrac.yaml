deptrac:
  # i updated this because it was looking to wrong path
  paths:
    - app

  layers:
    - name: Controllers
      collectors:
        - type: directory
          value: app/Http/Controllers
    - name: Models
      collectors:
        - type: directory
          value: app/Models
  ruleset:
    Controllers:
      - Models
